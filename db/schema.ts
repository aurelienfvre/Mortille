import { sqliteTable, text, integer, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const players = sqliteTable('arcade_players', { id: text('id').primaryKey(), tokenHash: text('token_hash').notNull(), pseudo: text('pseudo').notNull(), createdAt: integer('created_at').notNull() }, t => [uniqueIndex('idx_players_token').on(t.tokenHash)]);
export const runs = sqliteTable('arcade_runs', { id: text('id').primaryKey(), playerId: text('player_id').notNull().references(() => players.id), gameId: text('game_id').notNull(), levelId: text('level_id').notNull(), startedAt: integer('started_at').notNull(), finishedAt: integer('finished_at'), replayVersion: integer('replay_version').notNull().default(0), ticks: integer('ticks'), score: integer('score'), secrets: integer('secrets') }, t => [index('idx_runs_player_game_level').on(t.playerId, t.gameId, t.levelId), index('idx_runs_leaderboard').on(t.gameId, t.levelId, t.score), index('idx_runs_player_started').on(t.playerId, t.startedAt)]);
